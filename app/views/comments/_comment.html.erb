<div>
  <%= form_for :comment, url: comments_path do |f| %>
    <div>
      <div>
        <%= f.text_area :comment, placeholder: 'comment here ...', class: 'form-control' %>
      </div>
      <div><%= f.submit 'comment', class: 'btn btn-primary' %></div>
    </div>
  <% end %>
</div>